@using Data.DTO
@using Data.DTO.Product
@model IEnumerable<ProductListDTO>
@await Html.PartialAsync("_Carousel", null)

<!-- Searvices Start -->
@await Html.PartialAsync("_Searvices", null)
@await Html.PartialAsync("_OurProduct", Model)
@await Html.PartialAsync("_BestsellerProducts", (List<ProductBestsellerDTO>)ViewBag.BestSeller)
@* @await Html.PartialAsync("_ProductList") *@

@if (TempData["Success"] != null)
{
    <div class="alert alert-success text-center">
        @TempData["Success"]
    </div>
}


